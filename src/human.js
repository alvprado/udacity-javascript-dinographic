/**
 * Human class
 */
export class Human {
  /**
   * Creates an human from data
   */
  constructor({name, height, weight, diet}) {
    this.name = name;
    this.height = height;
    this.weight = weight;
    this.diet = diet;
  }
}
